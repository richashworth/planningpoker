---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - planningpoker-web/src/containers/ResultsChart.jsx
  - planningpoker-web/src/containers/Results.jsx
  - planningpoker-web/src/containers/Header.jsx
  - planningpoker-web/src/containers/GamePane.jsx
  - planningpoker-web/src/components/SchemeTile.jsx
  - planningpoker-web/src/components/NameInput.jsx
  - planningpoker-web/src/components/Logo.jsx
  - planningpoker-web/src/components/Footer.jsx
  - planningpoker-web/src/actions/__tests__/index.test.js
  - planningpoker-web/src/App.jsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "ESLint produces zero warnings for unused imports/variables across all listed files"
  artifacts: []
  key_links: []
---

<objective>
Remove unused `React` imports from 9 JSX/JS files and the unused `runThunk` variable from the action test file to eliminate ESLint warnings.

Purpose: Clean CI lint output — zero warnings from these files.
Output: 10 files edited, lint passes clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused React imports and unused runThunk variable</name>
  <files>
    planningpoker-web/src/App.jsx
    planningpoker-web/src/containers/ResultsChart.jsx
    planningpoker-web/src/containers/Results.jsx
    planningpoker-web/src/containers/Header.jsx
    planningpoker-web/src/containers/GamePane.jsx
    planningpoker-web/src/components/SchemeTile.jsx
    planningpoker-web/src/components/NameInput.jsx
    planningpoker-web/src/components/Logo.jsx
    planningpoker-web/src/components/Footer.jsx
    planningpoker-web/src/actions/__tests__/index.test.js
  </files>
  <action>
    In each of the 9 JSX files listed above, remove the `import React from 'react'` line. React 18 with the automatic JSX transform (configured via `@vitejs/plugin-react`) does not require an explicit React import.

    IMPORTANT: If any file uses `React.` explicitly (e.g., `React.useState`, `React.createContext`), do NOT remove the import — instead convert to named imports (e.g., `import { useState, createContext } from 'react'`). Check each file before removing.

    In `planningpoker-web/src/actions/__tests__/index.test.js`, find and remove the unused `runThunk` variable declaration. If it was imported, remove the import. If it was defined locally, remove the definition. Ensure no other code references it.
  </action>
  <verify>
    <automated>cd planningpoker-web && npx eslint src/App.jsx src/containers/ResultsChart.jsx src/containers/Results.jsx src/containers/Header.jsx src/containers/GamePane.jsx src/components/SchemeTile.jsx src/components/NameInput.jsx src/components/Logo.jsx src/components/Footer.jsx src/actions/__tests__/index.test.js</automated>
  </verify>
  <done>All 10 files pass ESLint with zero warnings for unused imports/variables. No functional regressions — app still builds and tests pass.</done>
</task>

</tasks>

<verification>
1. `cd planningpoker-web && npx eslint src/` — no warnings from any of the 10 files
2. `cd planningpoker-web && npm run build` — Vite build succeeds
3. `cd planningpoker-web && npx vitest run` — all frontend tests pass
</verification>

<success_criteria>
- Zero ESLint warnings for unused React imports and unused runThunk variable
- Frontend builds without errors
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/260407-x87-remove-unused-react-imports-and-unused-r/260407-x87-SUMMARY.md`
</output>
