---
phase: quick
plan: 260407-x1g
type: execute
wave: 1
depends_on: []
files_modified:
  - planningpoker-web/src/components/SchemeTile.jsx
autonomous: true
requirements: [TODO-002]
must_haves:
  truths:
    - "Scheme tile text is readable in both dark and light mode without squinting"
    - "Unselected scheme names, icons, and value previews all meet WCAG AA contrast (4.5:1 for text)"
    - "Selected tiles remain visually distinct from unselected tiles"
  artifacts:
    - path: "planningpoker-web/src/components/SchemeTile.jsx"
      provides: "High-contrast scheme selector cards"
  key_links: []
---

<objective>
Fix low contrast on estimation scheme selector cards in SchemeTile.jsx so all text meets WCAG AA minimum 4.5:1 contrast ratio in both dark and light themes.

Purpose: The scheme values text is nearly invisible (dark: #3f3f46 on #1c1c20 = ~1.3:1, light: #a1a1aa on #fff = ~2.6:1). Icons and secondary text also have poor contrast when unselected.
Output: Updated SchemeTile.jsx with accessible color values.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@planningpoker-web/src/components/SchemeTile.jsx
@planningpoker-web/src/theme.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase contrast on all SchemeTile text elements</name>
  <files>planningpoker-web/src/components/SchemeTile.jsx</files>
  <action>
Update three color values in SchemeTile.jsx for unselected tiles to meet WCAG AA 4.5:1 contrast ratio:

1. **Scheme values caption** (line 98, the `.scheme-values` Typography):
   - Dark mode: change `#3f3f46` to `#a1a1aa` (contrast ~5.3:1 on #1c1c20)
   - Light mode: change `#a1a1aa` to `#71717a` (contrast ~4.7:1 on #fff)

2. **Icon color** (line 64, the Icon sx):
   - Change from `text.disabled` to `text.secondary` for unselected state
   - Dark: `text.secondary` = `#a1a1aa` (~5.3:1 on #1c1c20)
   - Light: `text.secondary` = `#71717a` (~4.7:1 on #fff)

3. **Scheme name** (line 73, the scheme name Typography):
   - Unselected already uses `text.secondary` which is adequate in light mode (#71717a on #fff = 4.7:1)
   - But in dark mode `text.secondary` is `#a1a1aa` on `#1c1c20` = 5.3:1 -- this is fine, no change needed

Keep selected state colors unchanged (they use `text.primary` and `primary.main` which are already high contrast).

The selected values caption uses `text.disabled` -- change this to `text.secondary` as well so the preview values remain readable even on the selected tile.
  </action>
  <verify>
    <automated>cd /Users/richard/Projects/planningpoker/planningpoker-web && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
  - All unselected SchemeTile text uses colors with >= 4.5:1 contrast ratio against tile backgrounds
  - No hardcoded dark-mode color below #a1a1aa on dark backgrounds
  - No hardcoded light-mode color above #71717a on white backgrounds
  - Selected tiles remain visually distinct from unselected tiles
  </done>
</task>

</tasks>

<verification>
- Visual check: Visit /host page and verify all scheme tile text is clearly readable in both dark and light mode
- All existing tests pass (vitest + playwright)
</verification>

<success_criteria>
- Scheme selector cards have readable text in both themes
- WCAG AA 4.5:1 contrast ratio met for all text elements
- Selected/unselected distinction preserved
</success_criteria>

<output>
After completion, create `.planning/quick/260407-x1g-fix-todo-002-increase-contrast-on-estima/260407-x1g-SUMMARY.md`
</output>
