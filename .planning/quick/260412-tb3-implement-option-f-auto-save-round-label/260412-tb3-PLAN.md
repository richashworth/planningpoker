---
phase: 260412-tb3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - planningpoker-web/src/containers/Vote.jsx
  - planningpoker-web/src/containers/Results.jsx
  - planningpoker-web/tests/session-labels-csv.spec.js
autonomous: false
requirements:
  - Option-F-banner
  - Option-F-autosave
  - Option-F-export-relocation

must_haves:
  truths:
    - "Host no longer sees a Set button next to the round label input"
    - "Host label edits auto-save on blur and after a 1000ms typing pause (and still on Enter)"
    - "A transient 'Saved' indicator appears briefly after a successful label save"
    - "Host sees a prominent 'CURRENT ITEM' banner above the voting grid showing the label"
    - "Clicking the banner (or its pencil icon) switches it into an inline edit TextField"
    - "Non-hosts never see the edit affordance; banner is hidden when currentLabel is empty for non-hosts"
    - "Results header no longer contains Export CSV — only Consensus + Next Item remain on the right"
    - "A 'SESSION HISTORY · N rounds' row appears below the chart with Export CSV on the right"
    - "The session history row is hidden when there are no rounds and no current results"
    - "Playwright e2e tests pass against the new auto-save and relocated export UI"
  artifacts:
    - path: "planningpoker-web/src/containers/Vote.jsx"
      provides: "CURRENT ITEM banner + click-to-edit auto-save label input"
      contains: "CURRENT ITEM"
    - path: "planningpoker-web/src/containers/Results.jsx"
      provides: "Header without Export CSV; session history row with export below chart"
      contains: "SESSION HISTORY"
    - path: "planningpoker-web/tests/session-labels-csv.spec.js"
      provides: "Playwright coverage updated for auto-save semantics and relocated Export CSV"
  key_links:
    - from: "Vote.jsx banner click"
      to: "inline TextField edit mode"
      via: "local isEditing state + autoFocus"
      pattern: "isEditing"
    - from: "Vote.jsx debounced effect"
      to: "setLabel action"
      via: "setTimeout 1000ms + diff check against lastBroadcastLabel"
      pattern: "setLabel\\("
    - from: "Results.jsx session history row"
      to: "handleExportCsv"
      via: "onClick button below chart"
      pattern: "SESSION HISTORY"
---

<objective>
Implement Option F from docs/layout-mockups.html: auto-saving prominent "CURRENT ITEM" banner in Vote.jsx and relocated Export CSV under a session-history row in Results.jsx. Frontend-only change; no backend or new dependencies.

Purpose: Reduce friction on the host's primary flow (no Set click, no hunting for the item description) and co-locate Export CSV with the rounds history data it actually exports.
Output: Updated Vote.jsx, Results.jsx, and Playwright spec file; existing backend and actions unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/layout-mockups.html
@planningpoker-web/src/containers/Vote.jsx
@planningpoker-web/src/containers/Results.jsx
@planningpoker-web/src/actions/index.js
@planningpoker-web/src/reducers/reducer_game.js
@planningpoker-web/tests/session-labels-csv.spec.js

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From planningpoker-web/src/actions/index.js:
```js
// Thunk. Posts to /setLabel, dispatches SET_LABEL on success (or with error flag).
export function setLabel(userName, sessionId, label)
```

From planningpoker-web/src/reducers/reducer_game.js:
```js
// state.game.currentLabel — updated via LABEL_UPDATED (WS broadcast) and cleared on RESET_SESSION.
// state.game.isAdmin — host flag, gates all editing affordances.
```

From planningpoker-web/src/containers/Vote.jsx (current):
```js
// Local state: labelInput, lastBroadcastLabel, selected
// handleSubmit diff-checks labelInput vs lastBroadcastLabel and dispatches setLabel.
// useEffect syncs labelInput <- currentLabel when it changes via WS.
```

From planningpoker-web/src/containers/Results.jsx (current):
```js
// handleExportCsv — builds rounds+current, calls generateCsv + downloadCsv. KEEP AS-IS.
// disabled condition: rounds.length === 0 && results.length === 0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace Vote.jsx label input with auto-saving CURRENT ITEM banner</name>
  <files>planningpoker-web/src/containers/Vote.jsx</files>
  <behavior>
    - Host sees a banner above the cards grid with caption "CURRENT ITEM" and bold label text
    - Banner uses rgba(102,126,234,0.08) bg + 1px rgba(102,126,234,0.35) border + borderRadius 0.75 + p '12px 14px'
    - When currentLabel is empty, banner shows placeholder text "Add item description..." in text.secondary
    - Banner has an MUI EditIcon IconButton on the right (aria-label "Edit current item")
    - Banner is keyboard-focusable (tabIndex={0}) and role="button"; Enter/Space enters edit mode
    - Clicking the banner or the pencil swaps to an inline TextField (variant="standard", autoFocus, fullWidth)
    - TextField no longer has a Set button endAdornment — it is removed entirely
    - Typing dispatches setLabel after a 1000ms debounce if labelInput !== lastBroadcastLabel
    - onBlur dispatches setLabel if labelInput !== lastBroadcastLabel, then exits edit mode back to banner
    - Enter key still dispatches setLabel and then exits edit mode
    - After each successful dispatch, a small "✓ Saved" chip/Typography appears next to or under the banner for ~1500ms then hides (use useState + setTimeout; clear timeout on unmount)
    - Debounce timer is cleared on unmount and on every new keystroke before scheduling a new one
    - For non-admin users, banner is display-only (no pencil, no click-to-edit, no tabIndex) and is hidden entirely when currentLabel is empty (preserves current behavior)
  </behavior>
  <action>
    1. Import EditIcon from '@mui/icons-material/Edit' and IconButton from '@mui/material/IconButton'. Drop the unused InputAdornment and Button imports if no longer referenced.
    2. Add local state: const [isEditing, setIsEditing] = useState(false); const [justSaved, setJustSaved] = useState(false).
    3. Keep the existing labelInput / lastBroadcastLabel state and the currentLabel sync useEffect.
    4. Add a useRef for the debounce timer (useRef(null)) and a useRef for the savedTimer. Clear both in a cleanup useEffect.
    5. Extract a commitLabel() helper:
       - if labelInput === lastBroadcastLabel, return without dispatching
       - dispatch(setLabel(playerName, sessionId, labelInput))
       - setLastBroadcastLabel(labelInput)
       - setJustSaved(true); clearTimeout(savedTimer.current); savedTimer.current = setTimeout(() => setJustSaved(false), 1500)
    6. Add an onChange handler that calls setLabelInput and schedules a debounced commit: clearTimeout(debounceTimer.current); debounceTimer.current = setTimeout(commitLabelRef.current, 1000). Use a ref to always call the latest commitLabel (closure over latest state), or include labelInput in a useEffect-based debounce.
       - Preferred: replace the manual timer with a useEffect that watches labelInput and triggers a 1000ms debounced commit when isEditing is true and labelInput !== lastBroadcastLabel. This avoids stale closures.
    7. Replace the existing host TextField block with JSX that renders EITHER the banner OR the TextField based on isEditing:
       - Banner (isAdmin && !isEditing): Box with sx {{ mt: 0, mb: 3, px: 1.75, py: 1.5, bgcolor: 'rgba(102,126,234,0.08)', border: '1px solid rgba(102,126,234,0.35)', borderRadius: 0.75, display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: 1, cursor: 'pointer', '&:hover': { borderColor: 'primary.main' } }}, role="button", tabIndex={0}, aria-label="Edit current item", onClick={() => setIsEditing(true)}, onKeyDown handles Enter/Space.
         Inside: left Box with Typography variant="caption" sx={{ letterSpacing: '0.08em', color: 'text.secondary', display: 'block', fontSize: '0.7rem' }}>CURRENT ITEM</Typography> and Typography variant="body1" sx={{ fontWeight: 600 }}>{currentLabel || 'Add item description...'}</Typography> (use text.secondary + fontStyle italic when empty).
         Right: IconButton size="small" aria-label="Edit current item" onClick={(e) => { e.stopPropagation(); setIsEditing(true) }}> <EditIcon fontSize="small" /> </IconButton>.
       - Edit mode (isAdmin && isEditing): TextField variant="standard" fullWidth size="small" autoFocus placeholder="Round label (optional)" value={labelInput} onChange={(e) => setLabelInput(e.target.value)} onBlur={() => { commitLabel(); setIsEditing(false) }} onKeyDown={(e) => { if (e.key === 'Enter') { e.preventDefault(); commitLabel(); setIsEditing(false) } else if (e.key === 'Escape') { setLabelInput(lastBroadcastLabel); setIsEditing(false) } }} inputProps={{ maxLength: 100 }}. NO endAdornment.
       - Non-admin (existing behavior): currentLabel && render a display-only banner variant OR keep the current italic Typography. Use the display-only banner (same styling minus the pencil and cursor/hover) for visual consistency with the host view, but skip entirely when !currentLabel.
    8. Render the transient saved indicator below or beside the banner: {justSaved && <Typography variant="caption" sx={{ color: 'success.main', ml: 1 }}>✓ Saved</Typography>}. Place it so it doesn't shift layout (e.g. absolutely positioned or inside the banner's right cluster).
    9. Ensure the existing mb: 3 spacing above the cards grid is preserved (the banner replaces the old label Box wrapper, so apply mb: 3 to the banner wrapper).
    10. Use 2-space indent, single quotes, no semicolons, camelCase handlers per CLAUDE.md.
  </action>
  <verify>
    <automated>cd planningpoker-web && npm run lint && npm run format:check</automated>
  </verify>
  <done>
    Vote.jsx compiles clean, lint + prettier pass, Set button is gone, banner renders with edit affordance, auto-save fires on blur/debounce/Enter, transient Saved indicator appears briefly after dispatch.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Relocate Export CSV in Results.jsx into a session-history row below the chart</name>
  <files>planningpoker-web/src/containers/Results.jsx</files>
  <behavior>
    - The header row (above the chart) contains only the Results heading + Consensus Select (host) or Consensus Typography (non-host), and the Next Item button (host only)
    - The Export CSV button is removed from the header row entirely
    - A new session history row is rendered inside the existing Results Box, BELOW the grid containing the chart and ResultsTable
    - The row has a 1px divider top border, pt: 2, mt: 2, display flex, alignItems center, justifyContent space-between
    - Left: Typography variant="caption" color="text.secondary" with content "SESSION HISTORY · {rounds.length} rounds" (use "round"/"rounds" pluralization; rounds.length may be 0)
    - Right: Button variant="outlined" size="small" startIcon={<DownloadIcon />} onClick={handleExportCsv} with text "Export CSV", disabled iff (rounds.length === 0 && results.length === 0)
    - The entire session history row is hidden when rounds.length === 0 && results.length === 0 (no exports available)
    - Non-hosts do NOT see Next Item (unchanged) but DO see the session history row with Export CSV enabled when data exists, matching current behavior where Export CSV was host-only gated by isAdmin — PRESERVE the isAdmin gate on the whole row to keep export host-only
    - The existing "Round N" caption above the chart remains untouched
  </behavior>
  <action>
    1. Remove the Export CSV Button JSX from the header row's right cluster. The right cluster now contains only the Next Item Button (still wrapped in isAdmin gate). If Next Item is the only child, the wrapping Box can stay for layout parity.
    2. Keep DownloadIcon import — it's now used below the chart instead.
    3. After the closing tag of the <Box sx={{ display: 'grid', gridTemplateColumns: ... }}> that holds the chart + ResultsTable, add a new JSX block (still inside the outer Results Box):
       ```jsx
       {isAdmin && (rounds.length > 0 || results.length > 0) && (
         <Box
           sx={{
             mt: 2,
             pt: 2,
             borderTop: '1px solid',
             borderColor: 'divider',
             display: 'flex',
             alignItems: 'center',
             justifyContent: 'space-between',
             gap: 2,
             flexWrap: 'wrap',
           }}
         >
           <Typography
             variant="caption"
             color="text.secondary"
             sx={{ letterSpacing: '0.08em', textTransform: 'uppercase', fontSize: '0.7rem' }}
           >
             Session history · {rounds.length} {rounds.length === 1 ? 'round' : 'rounds'}
           </Typography>
           <Button
             variant="outlined"
             size="small"
             startIcon={<DownloadIcon />}
             onClick={handleExportCsv}
             disabled={rounds.length === 0 && results.length === 0}
           >
             Export CSV
           </Button>
         </Box>
       )}
       ```
    4. Note: the outer Box will naturally contain this row since Results.jsx returns a single <Box> wrapper. Verify by reading the final return structure — the new row should be a sibling of the chart+table grid, still inside the outer Box.
    5. Ensure the mockup's "SESSION HISTORY" uppercase styling is applied via sx textTransform uppercase + letterSpacing (the string itself can be mixed case — the CSS will uppercase it).
    6. Use 2-space indent, single quotes, no semicolons per CLAUDE.md.
  </action>
  <verify>
    <automated>cd planningpoker-web && npm run lint && npm run format:check && npm run build</automated>
  </verify>
  <done>
    Results.jsx header has no Export CSV button, session history row renders below chart with Export CSV, row hidden when no data exists, row gated on isAdmin, vite build succeeds.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update Playwright session-labels-csv.spec.js for auto-save and relocated export</name>
  <files>planningpoker-web/tests/session-labels-csv.spec.js</files>
  <behavior>
    - Tests that previously clicked the "Set round label" button now rely on auto-save (blur or debounce) or Enter key
    - Test titled "Set button is disabled when input matches last broadcast" is removed OR rewritten to assert the banner UI and that no dispatch happens when input matches lastBroadcastLabel (using network request counting)
    - Test titled "no /setLabel network request fires while typing — only on Set" is rewritten as "no /setLabel network request fires before debounce settles; exactly one fires after pause" — asserts at most one POST /setLabel within 1200ms of settled typing
    - Test titled "Set button explicitly broadcasts label to non-host" is rewritten to use blur or Enter as the commit trigger
    - Test titled "Empty Set submission clears the broadcast label" uses blur or Enter
    - Export CSV test (around line 416) still passes — the button is now below the chart but still role=button name "Export CSV"
    - Tests that fill the round label input must either: press Enter after filling, or call .blur() on the input, or wait >1100ms for the debounce, before asserting downstream state
    - All other assertions about label broadcasting, CSV contents, and round progression remain intact
    - Tests must enter edit mode before the label input is accessible: click the banner (role=button name "Edit current item") or the placeholder text, then fill the input
  </behavior>
  <action>
    1. Read the full test file to understand each test's flow before editing.
    2. For each test that uses `getByPlaceholder('Round label (optional)')`, add a click on the banner first to enter edit mode. Use: `await hostPage.getByRole('button', { name: 'Edit current item' }).first().click()`. The banner is visible by default, so this is always the entry point.
    3. Replace all `getByRole('button', { name: 'Set round label' }).click()` with a commit trigger:
       - Preferred: `await labelInput.press('Enter')` — deterministic and fast
       - Alternative where blur semantics need testing: `await labelInput.blur()`
    4. Delete the test "Set button is disabled when input matches last broadcast" — the Set button no longer exists, and the "no dispatch when unchanged" behavior is now covered by the auto-save test.
    5. Rewrite the test "no /setLabel network request fires while typing — only on Set":
       - New title: "no /setLabel fires while typing; exactly one fires after debounce settles"
       - Count POST /setLabel requests via page.on('request') filter
       - Click banner to enter edit mode
       - Type "Hello" character-by-character with 100ms delays (total <500ms, under debounce threshold)
       - Assert request count is 0 immediately after typing completes
       - Wait 1200ms
       - Assert request count is 1
       - Then optionally test Enter key also commits cleanly
    6. Rewrite "Set button explicitly broadcasts label to non-host": rename to "label broadcasts to non-host on Enter key", use press('Enter') as the commit trigger.
    7. For "Empty Set submission clears the broadcast label": rename to "empty label on Enter clears broadcast", use press('Enter').
    8. For the Export CSV test: no locator change needed — `getByRole('button', { name: 'Export CSV' })` still resolves. But the test may need to scroll the button into view since it moved below the chart. Add `await exportBtn.scrollIntoViewIfNeeded()` before the click if needed.
    9. Do not introduce new deps. Keep existing imports and helpers.
    10. Lint + format after edits.
  </action>
  <verify>
    <automated>cd planningpoker-web && npm run lint && npm run format:check</automated>
  </verify>
  <done>
    Spec file lint-clean; all previous Set-button references removed or rewritten; auto-save semantics tested with network counting; Export CSV test updated for new location.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — run full build + e2e and visually confirm Option F</name>
  <what-built>
    Option F: auto-saving CURRENT ITEM banner in Vote.jsx (no Set button, debounced save on typing, save on blur/Enter, transient Saved indicator, click-to-edit) and relocated Export CSV into a SESSION HISTORY row below the chart in Results.jsx. Playwright tests updated for the new UX.
  </what-built>
  <how-to-verify>
    1. Run the full build and unit tests:
       ```
       cd planningpoker-web && npm run build
       cd .. && ./gradlew planningpoker-api:test
       ```
       Both must succeed.
    2. Start the backend: `./gradlew planningpoker-api:bootRun` in one terminal.
    3. In another terminal run e2e: `cd planningpoker-web && npx playwright test tests/session-labels-csv.spec.js`. All tests must pass.
    4. Start the frontend dev server: `cd planningpoker-web && npm run dev`. Open http://localhost:3000 in two browser windows (one host, one join) using the same session ID.
    5. As host on /game:
       a. Confirm a banner shows above the voting grid with caption "CURRENT ITEM" and placeholder "Add item description..." (italic, muted).
       b. Confirm the old Set button is gone.
       c. Click the banner. An inline TextField should appear, focused.
       d. Type "AUTH-142 — OAuth login with Google". Stop typing. After ~1s, a "✓ Saved" indicator should flash briefly (~1.5s), and the non-host window should see the label.
       e. Click elsewhere (blur). The banner returns with the bold label and a pencil icon on the right.
       f. Click the pencil — it should re-enter edit mode with autofocus.
       g. Edit, then press Enter — label updates, indicator flashes.
       h. Press Tab to the banner; press Enter — it should enter edit mode via keyboard.
    6. As non-host: confirm the banner is display-only (no pencil, not clickable, not focusable) and hidden entirely when host clears the label.
    7. Cast a vote as host. In the Results section:
       a. Confirm the header row contains Results + Consensus Select + Next Item only — NO Export CSV.
       b. Scroll down (or look below the chart+table grid). Confirm a row with "SESSION HISTORY · 0 rounds" on the left and an Export CSV outlined button on the right. The top border divider should be visible.
       c. Click Export CSV. A CSV file should download.
       d. Click Next Item. Complete another round, vote again. Confirm "SESSION HISTORY · 1 round" (singular) is shown.
    8. Toggle dark/light mode. Confirm the banner background tint remains readable in both modes.
    9. Refresh the host page after logging out. Confirm no session history row is shown on a fresh session (rounds.length === 0 && results.length === 0).
  </how-to-verify>
  <resume-signal>Type "approved" if the banner, auto-save, Saved indicator, and relocated Export CSV all behave per Option F. Otherwise describe issues and I'll revise.</resume-signal>
</task>

</tasks>

<verification>
- Lint + prettier pass on both Vote.jsx and Results.jsx
- `npm run build` succeeds
- Playwright `session-labels-csv.spec.js` fully passes against the new UX
- Visual inspection matches docs/layout-mockups.html Option F
- No backend or action changes required
</verification>

<success_criteria>
- No "Set" button visible anywhere in Vote.jsx output
- Label auto-saves on blur, 1000ms debounce during typing, and Enter
- Transient "✓ Saved" indicator appears after each successful dispatch
- "CURRENT ITEM" banner renders above the voting grid with click-to-edit + pencil
- Non-hosts see display-only banner or nothing when empty
- Results header contains only Results + Consensus + Next Item
- "SESSION HISTORY · N rounds" row renders below the chart with Export CSV
- Session history row hidden when no rounds and no current results
- All Playwright tests in session-labels-csv.spec.js pass
</success_criteria>

<output>
After completion, create `.planning/quick/260412-tb3-implement-option-f-auto-save-round-label/260412-tb3-SUMMARY.md`
</output>
