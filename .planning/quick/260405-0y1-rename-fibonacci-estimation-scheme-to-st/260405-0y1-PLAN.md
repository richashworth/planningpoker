---
phase: quick-260405-0y1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - planningpoker-api/src/main/java/com/richashworth/planningpoker/model/SchemeType.java
  - planningpoker-api/src/main/java/com/richashworth/planningpoker/service/SessionManager.java
  - planningpoker-api/src/main/java/com/richashworth/planningpoker/controller/GameController.java
  - planningpoker-api/src/test/java/com/richashworth/planningpoker/model/SchemeTypeTest.java
  - planningpoker-api/src/test/java/com/richashworth/planningpoker/controller/GameControllerTest.java
  - planningpoker-api/src/test/java/com/richashworth/planningpoker/controller/VoteControllerTest.java
  - planningpoker-api/src/test/java/com/richashworth/planningpoker/service/SessionManagerTest.java
  - planningpoker-web/src/config/Constants.js
  - planningpoker-web/src/reducers/reducer_game.js
  - planningpoker-web/src/reducers/__tests__/reducer_game.test.js
  - planningpoker-web/src/pages/CreateGame.jsx
autonomous: true
must_haves:
  truths:
    - "The enum constant is STORY_POINTS, not FIBONACCI"
    - "All string references use 'story_points' (lowercase) instead of 'fibonacci'"
    - "The UI label reads 'Story Points' not 'Fibonacci'"
    - "The card values remain identical (0, 0.5, 1, 2, 3, 5, 8, 13, 20, 50, 100, infinity)"
    - "All backend and frontend tests pass"
    - "Backward-compat: fromString('fibonacci') still resolves to STORY_POINTS"
  artifacts:
    - path: "planningpoker-api/src/main/java/com/richashworth/planningpoker/model/SchemeType.java"
      contains: "STORY_POINTS"
    - path: "planningpoker-web/src/config/Constants.js"
      contains: "story_points"
    - path: "planningpoker-web/src/pages/CreateGame.jsx"
      contains: "Story Points"
  key_links:
    - from: "CreateGame.jsx"
      to: "GameController.java"
      via: "schemeType field in POST /createSession"
      pattern: "story_points"
    - from: "SchemeType.fromString"
      to: "SchemeType.STORY_POINTS"
      via: "case-insensitive name match + backward compat alias"
---

<objective>
Rename the FIBONACCI estimation scheme to STORY_POINTS across the entire codebase. The card values stay identical — only the name/label changes. Add backward compatibility so "fibonacci" still resolves via fromString.

Purpose: "Story Points" is the accurate name for this scale; "Fibonacci" describes the mathematical sequence, not the estimation practice.
Output: Renamed enum, updated string references, updated UI label, all tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@planningpoker-api/src/main/java/com/richashworth/planningpoker/model/SchemeType.java
@planningpoker-web/src/config/Constants.js
@planningpoker-web/src/pages/CreateGame.jsx
@planningpoker-web/src/reducers/reducer_game.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename backend enum and add backward-compat alias</name>
  <files>
    planningpoker-api/src/main/java/com/richashworth/planningpoker/model/SchemeType.java,
    planningpoker-api/src/main/java/com/richashworth/planningpoker/service/SessionManager.java,
    planningpoker-api/src/main/java/com/richashworth/planningpoker/controller/GameController.java,
    planningpoker-api/src/test/java/com/richashworth/planningpoker/model/SchemeTypeTest.java,
    planningpoker-api/src/test/java/com/richashworth/planningpoker/controller/GameControllerTest.java,
    planningpoker-api/src/test/java/com/richashworth/planningpoker/controller/VoteControllerTest.java,
    planningpoker-api/src/test/java/com/richashworth/planningpoker/service/SessionManagerTest.java
  </files>
  <action>
    1. In SchemeType.java:
       - Rename `FIBONACCI(List.of(...))` to `STORY_POINTS(List.of(...))` — same values
       - In `fromString()`, add a backward-compat check: if the input equals "fibonacci" (case-insensitive), return STORY_POINTS. This goes BEFORE the existing loop, or as a special case. This ensures existing sessions/clients using "fibonacci" still work.

    2. In SessionManager.java line 37:
       - Change `new SchemeConfig("fibonacci", ...)` to `new SchemeConfig("story_points", ...)`

    3. In GameController.java line 118:
       - Change the default from `"fibonacci"` to `"story_points"`

    4. In SchemeTypeTest.java:
       - Replace all `SchemeType.FIBONACCI` references with `SchemeType.STORY_POINTS`
       - Replace `fromString("fibonacci")` / `fromString("FIBONACCI")` assertions to test `SchemeType.STORY_POINTS` as the result
       - Add a NEW test: `fromString("fibonacci")` still returns `SchemeType.STORY_POINTS` (backward compat)
       - Add a NEW test: `fromString("FIBONACCI")` still returns `SchemeType.STORY_POINTS` (backward compat)
       - Update SchemeConfig test: change `"fibonacci"` to `"story_points"` in the config constructor and assertEquals

    5. In GameControllerTest.java:
       - Replace all `"fibonacci"` string literals in SchemeConfig constructors with `"story_points"`
       - Replace `assertEquals("fibonacci", response.schemeType())` with `assertEquals("story_points", response.schemeType())`
       - Rename local variable `fibValues` to `storyPointValues` for clarity

    6. In VoteControllerTest.java:
       - Rename `FIBONACCI_VALUES` constant to `STORY_POINT_VALUES` — same list values
       - Update all references to use the new name

    7. In SessionManagerTest.java:
       - Replace all `"fibonacci"` strings in SchemeConfig constructors with `"story_points"`
       - Replace `assertEquals("fibonacci", retrieved.schemeType())` with `assertEquals("story_points", ...)`
       - Update resolveValues calls from `"fibonacci"` to `"story_points"`
  </action>
  <verify>
    <automated>cd /Users/richard/Projects/planningpoker && ./gradlew planningpoker-api:test</automated>
  </verify>
  <done>Backend enum is STORY_POINTS, all string defaults are "story_points", fromString("fibonacci") still works for backward compat, all backend tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Rename frontend constants, state, and UI label</name>
  <files>
    planningpoker-web/src/config/Constants.js,
    planningpoker-web/src/reducers/reducer_game.js,
    planningpoker-web/src/reducers/__tests__/reducer_game.test.js,
    planningpoker-web/src/pages/CreateGame.jsx
  </files>
  <action>
    1. In Constants.js:
       - Rename the `fibonacci` key in the ESTIMATION_SCHEMES object to `story_points` — same array values

    2. In reducer_game.js line 9:
       - Change initial state `schemeType: 'fibonacci'` to `schemeType: 'story_points'`

    3. In reducer_game.test.js line 7:
       - Change `schemeType: 'fibonacci'` to `schemeType: 'story_points'` in the expected initial state

    4. In CreateGame.jsx:
       - Line 41: Change `useState('fibonacci')` to `useState('story_points')`
       - Line 108: Change `<ToggleButton value="fibonacci">Fibonacci</ToggleButton>` to `<ToggleButton value="story_points">Story Points</ToggleButton>`
  </action>
  <verify>
    <automated>cd /Users/richard/Projects/planningpoker/planningpoker-web && npx vitest run</automated>
  </verify>
  <done>Frontend uses "story_points" everywhere, UI toggle reads "Story Points", frontend tests pass.</done>
</task>

</tasks>

<verification>
1. `./gradlew planningpoker-api:test` — all backend tests green
2. `cd planningpoker-web && npx vitest run` — all frontend tests green
3. Grep confirms no remaining "fibonacci" references in source code (except backward-compat alias in SchemeType.fromString and test for that alias)
</verification>

<success_criteria>
- The enum is `SchemeType.STORY_POINTS` with the same card values
- All default strings are "story_points"
- The UI button reads "Story Points"
- `SchemeType.fromString("fibonacci")` returns STORY_POINTS (backward compat)
- All backend and frontend tests pass
- No unintended changes to card values
</success_criteria>

<output>
After completion, create `.planning/quick/260405-0y1-rename-fibonacci-estimation-scheme-to-st/260405-0y1-SUMMARY.md`
</output>
