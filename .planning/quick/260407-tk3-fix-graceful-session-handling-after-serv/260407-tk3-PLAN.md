---
phase: quick
plan: 260407-tk3
type: execute
wave: 1
depends_on: []
files_modified:
  - planningpoker-web/src/pages/PlayGame.jsx
autonomous: true
must_haves:
  truths:
    - "After a server restart, reconnected WebSocket triggers session validation"
    - "If session no longer exists on backend, user is kicked with an explanatory message"
    - "Initial WebSocket connect does NOT trigger validation (only reconnects do)"
  artifacts:
    - path: "planningpoker-web/src/pages/PlayGame.jsx"
      provides: "wasConnected ref and reconnect-validation useEffect"
  key_links:
    - from: "PlayGame.jsx reconnect useEffect"
      to: "GET /sessionUsers"
      via: "axios call on reconnect detection"
      pattern: "axios\\.get.*sessionUsers"
---

<objective>
Detect WebSocket reconnection in PlayGame and validate the session still exists on the backend. If the server was restarted (session gone), gracefully kick the user with a clear message instead of leaving them on a broken game page.

Purpose: After a Railway deploy or server restart, users currently sit on a dead game page with no feedback. This adds reconnect detection so they get redirected to Welcome with a "server restarted" message.
Output: Updated PlayGame.jsx with reconnect-aware session validation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@planningpoker-web/src/pages/PlayGame.jsx
@planningpoker-web/src/actions/index.js
@planningpoker-web/src/hooks/useStomp.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reconnect detection and session validation to PlayGame</name>
  <files>planningpoker-web/src/pages/PlayGame.jsx</files>
  <action>
Add a `wasConnected` ref using `useRef(false)`.

Add a new `useEffect` that watches the `connected` value from `useStomp`. The logic must be ordered carefully:

1. When `connected === true` AND `wasConnected.current === true`, this is a reconnect (not first connect). Call `axios.get(\`\${API_ROOT_URL}/sessionUsers?sessionId=\${sessionId}\`)`. On error (catch block), set `sessionStorage.setItem('pp-kicked-message', 'The session ended because the server was restarted.')` then `dispatch(kicked())`.
2. When `connected === true` (any time, after the check above), set `wasConnected.current = true`.

The order matters: check `wasConnected.current` BEFORE setting it to true so the initial connect does not trigger validation.

Dependencies on the dep array: `connected`, `sessionId`, `dispatch`.

No other files need changes. `kicked` is already imported. `axios` and `API_ROOT_URL` are already imported. The existing `useEffect` watching `isUserRegistered` already handles the redirect to `/` when `kicked()` clears registration state.
  </action>
  <verify>
    <automated>cd /Users/richard/Projects/planningpoker/planningpoker-web && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
PlayGame.jsx contains a wasConnected ref and a useEffect that: (a) on reconnect, validates session via GET /sessionUsers, (b) on validation failure dispatches kicked() with a sessionStorage message, (c) does NOT trigger on initial connect. All existing tests pass.
  </done>
</task>

</tasks>

<verification>
- Read PlayGame.jsx and confirm `wasConnected` ref exists, initialized to `false`
- Confirm the useEffect checks `wasConnected.current === true` before the line that sets it to `true`
- Confirm axios error handler sets sessionStorage message and dispatches `kicked()`
- Existing frontend tests pass: `cd planningpoker-web && npx vitest run`
</verification>

<success_criteria>
- PlayGame detects WebSocket reconnect (distinguished from initial connect)
- On reconnect, validates session exists via GET /sessionUsers
- If session gone, user sees "server was restarted" message on Welcome page
- No regression in existing tests
</success_criteria>

<output>
After completion, create `.planning/quick/260407-tk3-fix-graceful-session-handling-after-serv/260407-tk3-SUMMARY.md`
</output>
