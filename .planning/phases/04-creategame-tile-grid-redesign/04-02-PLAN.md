---
phase: 04-creategame-tile-grid-redesign
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - planningpoker-web/tests/planning-poker.spec.js
autonomous: false
requirements: [BWC-02]

must_haves:
  truths:
    - "All existing Playwright e2e tests pass after the tile grid redesign"
    - "Scheme selection tests use updated selectors that work with tile grid"
    - "Custom scheme test still fills inline input and creates game with custom values"
    - "Meta-card toggle test still disables unsure and coffee switches"
  artifacts:
    - path: "planningpoker-web/tests/planning-poker.spec.js"
      provides: "Updated e2e tests with tile-compatible selectors"
      contains: "scheme-tile"
  key_links:
    - from: "planningpoker-web/tests/planning-poker.spec.js"
      to: "planningpoker-web/src/components/SchemeTile.jsx"
      via: "data-testid selectors"
      pattern: "scheme-tile"
---

<objective>
Update Playwright e2e tests to work with the new tile grid selectors and verify all tests pass.

Purpose: The ToggleButtonGroup used `<button>` elements with scheme names as text. Tiles use `data-testid` and `role="option"` attributes. Tests need updated selectors to find and click scheme tiles.

Output: All 15+ existing e2e tests passing with the redesigned CreateGame page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-creategame-tile-grid-redesign/04-01-SUMMARY.md
@planningpoker-web/tests/planning-poker.spec.js
@planningpoker-web/src/components/SchemeTile.jsx

<interfaces>
<!-- SchemeTile data-testid pattern from Plan 01 -->
Each scheme tile has: data-testid="scheme-tile-{key}" where key is one of: fibonacci, tshirt, simple, time, custom
Each scheme tile has: role="option" and aria-selected={boolean}

<!-- Current e2e selectors that WILL BREAK (from ToggleButtonGroup) -->
Tests that click scheme buttons use:
  page.getByRole('button', { name: 'T-shirt' })   // ToggleButton had button role
  page.getByRole('button', { name: 'Simple' })     // ToggleButton had button role
  page.getByRole('button', { name: 'Custom' })     // ToggleButton had button role

These must change to:
  page.getByTestId('scheme-tile-tshirt')
  page.getByTestId('scheme-tile-simple')
  page.getByTestId('scheme-tile-custom')

<!-- Selectors that should NOT change -->
  page.getByLabel('Name')                         // NameInput unchanged
  page.getByRole('button', { name: 'Start Game' }) // Submit button unchanged
  page.getByLabel('Custom Values')                 // TextField label unchanged
  page.getByText('Include ? (unsure)')             // Switch label unchanged
  page.getByText(/Include.*break/)                 // Switch label unchanged
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update e2e test selectors for tile grid</name>
  <files>planningpoker-web/tests/planning-poker.spec.js</files>
  <read_first>
    planningpoker-web/tests/planning-poker.spec.js
    planningpoker-web/src/components/SchemeTile.jsx
    planningpoker-web/src/pages/CreateGame.jsx
  </read_first>
  <action>
Update the Playwright e2e tests in `planningpoker-web/tests/planning-poker.spec.js`. Only change selectors that targeted ToggleButton elements — do NOT change test logic, assertions, or tests that don't interact with the scheme selector.

**Specific changes required:**

1. In "Estimation Schemes" > "T-shirt scheme shows correct cards" test (line ~240):
   - Change: `await page.getByRole('button', { name: 'T-shirt' }).click();`
   - To: `await page.getByTestId('scheme-tile-tshirt').click();`

2. In "Estimation Schemes" > "Simple scheme shows correct cards" test (line ~256):
   - Change: `await page.getByRole('button', { name: 'Simple' }).click();`
   - To: `await page.getByTestId('scheme-tile-simple').click();`

3. In "Estimation Schemes" > "Custom scheme shows user-defined cards" test (line ~272):
   - Change: `await page.getByRole('button', { name: 'Custom' }).click();`
   - To: `await page.getByTestId('scheme-tile-custom').click();`
   - The `page.getByLabel('Custom Values').fill('Easy, Medium, Hard')` stays unchanged (TextField label is the same).

4. In "Estimation Schemes" > "disabling meta-card toggles hides ? and coffee cards" test (line ~285):
   - No changes needed — this test does not click a scheme selector (uses default fibonacci).
   - The switch selectors `page.getByText('Include ? (unsure)')` and `page.getByText(/Include.*break/)` are unchanged.

5. All other tests (Welcome, Host a Game, Join, Logout, Dark/Light, Multi-User, Copy Session ID):
   - No changes — they don't interact with the scheme selector.
   - The `hostGame` helper just fills name and clicks "Start Game" with default fibonacci — no scheme selection needed.

Do NOT add new tests in this task. Do NOT change the test descriptions. Do NOT modify assertions. Only update the 3 selectors listed above.
  </action>
  <verify>
    <automated>cd /Users/richard/Projects/planningpoker/planningpoker-web && grep -c "getByTestId('scheme-tile-" tests/planning-poker.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - planning-poker.spec.js contains exactly 3 occurrences of `getByTestId('scheme-tile-`
    - planning-poker.spec.js contains `getByTestId('scheme-tile-tshirt')`
    - planning-poker.spec.js contains `getByTestId('scheme-tile-simple')`
    - planning-poker.spec.js contains `getByTestId('scheme-tile-custom')`
    - planning-poker.spec.js does NOT contain `getByRole('button', { name: 'T-shirt' })`
    - planning-poker.spec.js does NOT contain `getByRole('button', { name: 'Simple' })`
    - planning-poker.spec.js does NOT contain `getByRole('button', { name: 'Custom' })`
    - planning-poker.spec.js still contains `getByRole('button', { name: 'Start Game' })` (unchanged)
    - planning-poker.spec.js still contains `getByLabel('Custom Values')` (unchanged)
    - planning-poker.spec.js still contains `Include ? (unsure)` (unchanged)
    - Total test count is unchanged (same number of `test(` calls)
  </acceptance_criteria>
  <done>Three e2e test selectors updated from ToggleButton role queries to data-testid queries matching new tile grid. All other tests unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification</name>
  <files>planningpoker-web/src/pages/CreateGame.jsx, planningpoker-web/src/components/SchemeTile.jsx</files>
  <action>
Human verification checkpoint. No code changes — the user visually inspects the completed tile grid redesign and runs the full e2e suite.

Verification steps are listed in how-to-verify below. The executor should present these steps to the user and wait for approval.
  </action>
  <what-built>
    Complete CreateGame tile grid redesign:
    - Scheme tiles with icons, names, descriptions, and sample value chips
    - 2-column grid on desktop, 3-column icon-only on mobile
    - Custom tile spans full width with inline input
    - Toggle switches for Unsure and Break extras
    - Card Preview section removed
  </what-built>
  <how-to-verify>
    Prerequisites: Backend must be running on port 9000 (`./gradlew planningpoker-api:bootRun`), and frontend dev server on port 3000 (`cd planningpoker-web && npm run dev`).

    1. Open http://localhost:3000/host in a desktop browser (width 600px+)
       - Verify: 4 scheme tiles appear in a 2x2 grid (Fibonacci, T-shirt, Simple, Time)
       - Verify: Custom tile appears below spanning full width
       - Verify: Each tile shows emoji icon, name, description text, and sample value chips
       - Verify: Fibonacci tile is pre-selected with highlighted border and checkmark icon
       - Verify: No "Card Preview" section exists

    2. Click the T-shirt tile
       - Verify: T-shirt tile gets highlighted border and checkmark
       - Verify: Fibonacci tile loses its highlight and checkmark
       - Verify: T-shirt tile shows chips: XS, S, M, L, XL, XXL

    3. Click the Custom tile
       - Verify: Custom tile gets highlighted and shows an inline text input
       - Verify: Type "A, B, C" in the input — no value chips shown (custom has no preset values)
       - Verify: The input shows helper text "Enter 2-20 comma-separated values"

    4. Toggle switches
       - Verify: "Include ? (unsure)" and "Include coffee (break)" switches are visible
       - Verify: Both are ON by default (switch track filled)
       - Verify: Clicking toggles them off/on with sliding indicator

    5. Submit with defaults
       - Enter a name, ensure Fibonacci is selected, click "Start Game"
       - Verify: Redirects to /game with standard Fibonacci cards (1,2,3,5,8,13,?,coffee)

    6. Responsive test: Resize browser to 400px width
       - Verify: Tiles switch to 3-column layout
       - Verify: Description text and value chips are hidden
       - Verify: Only emoji icon and scheme name visible in each tile

    7. Run e2e tests: `cd planningpoker-web && npx playwright test`
       - Verify: All tests pass (0 failures)
  </how-to-verify>
  <verify>
    <automated>cd /Users/richard/Projects/planningpoker/planningpoker-web && npx playwright test 2>&1 | tail -3</automated>
  </verify>
  <done>User confirms tile grid looks correct on desktop and mobile. All e2e tests pass. Game creation works end-to-end with default Fibonacci scheme.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test file changes only — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | e2e tests | accept | Test selectors changed to match new DOM structure. No security-relevant test weakening — same assertions, same coverage. |
</threat_model>

<verification>
1. `grep -c "getByTestId('scheme-tile-" planningpoker-web/tests/planning-poker.spec.js` returns 3
2. `grep "getByRole.*button.*T-shirt" planningpoker-web/tests/planning-poker.spec.js` returns nothing
3. All Playwright tests pass: `cd planningpoker-web && npx playwright test` exits 0
</verification>

<success_criteria>
- All existing Playwright e2e tests pass with 0 failures
- Scheme selector tests use data-testid selectors matching tile grid
- Visual verification confirms tile grid layout, responsive behavior, and toggle switches
- Host can create a game with default Fibonacci scheme (backwards compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/04-creategame-tile-grid-redesign/04-02-SUMMARY.md`
</output>
