---
phase: 10-frontend-modernization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - planningpoker-web/package.json
  - planningpoker-web/src/App.jsx
  - planningpoker-web/vite.config.js
autonomous: true
requirements:
  - CLN-01
  - CLN-03
  - UI-04

must_haves:
  truths:
    - "Redux store is created with configureStore from @reduxjs/toolkit — no createStore, applyMiddleware, or compose imports remain"
    - "redux-promise does not appear anywhere in the codebase"
    - "No subscription to /topic/items/${sessionId} exists in frontend source"
    - "Lazy-loaded routes show CircularProgress fallback during loading"
    - "All existing tests pass after migration"
  artifacts:
    - path: "planningpoker-web/src/App.jsx"
      provides: "Redux store via configureStore"
      contains: "configureStore"
    - path: "planningpoker-web/package.json"
      provides: "@reduxjs/toolkit dependency"
      contains: "@reduxjs/toolkit"
    - path: "planningpoker-web/vite.config.js"
      provides: "Updated manualChunks with @reduxjs/toolkit"
      contains: "@reduxjs/toolkit"
  key_links:
    - from: "planningpoker-web/src/App.jsx"
      to: "planningpoker-web/src/reducers/index.js"
      via: "import reducer passed to configureStore"
      pattern: "configureStore.*reducer"
---

<objective>
Migrate the Redux store from legacy `createStore` to Redux Toolkit's `configureStore`, verify the dead WebSocket subscription (CLN-01) and Suspense fallback (UI-04) are already satisfied, and update Vite chunk config.

Purpose: Modernize the Redux store setup, eliminating deprecated patterns (`createStore`, manual DevTools wiring, explicit thunk middleware). Two of the three requirements (CLN-01, UI-04) are already satisfied by existing code — this plan verifies them and addresses CLN-03.

Output: Updated `App.jsx` with `configureStore`, updated `vite.config.js` chunks, `@reduxjs/toolkit` installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-frontend-modernization/10-CONTEXT.md
@.planning/phases/10-frontend-modernization/10-RESEARCH.md

@planningpoker-web/src/App.jsx
@planningpoker-web/src/reducers/index.js
@planningpoker-web/vite.config.js
@planningpoker-web/package.json

<interfaces>
<!-- Executor needs to know what the root reducer exports -->

From planningpoker-web/src/reducers/index.js:
```javascript
import { combineReducers } from 'redux'
// ... individual reducer imports ...
export default combineReducers({ game, results, users, vote, notification, rounds })
```

The default export is a combined reducer function — passed directly to configureStore({ reducer }).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install RTK and migrate store setup</name>
  <files>planningpoker-web/package.json, planningpoker-web/src/App.jsx, planningpoker-web/vite.config.js</files>
  <read_first>
    - planningpoker-web/src/App.jsx (current store setup — lines 1-31)
    - planningpoker-web/package.json (current dependencies)
    - planningpoker-web/vite.config.js (manualChunks config)
    - planningpoker-web/src/reducers/index.js (root reducer export)
  </read_first>
  <action>
    **Step 1 — Install @reduxjs/toolkit:**
    ```bash
    cd planningpoker-web && npm install @reduxjs/toolkit
    ```

    **Step 2 — Update App.jsx store setup (per D-01, D-04):**

    Remove these lines:
    - Line 5: `import { applyMiddleware, compose, createStore } from 'redux'`
    - Line 6: `import thunk from 'redux-thunk'`
    - Line 26: `const middleware = [thunk]`
    - Lines 28-29: the eslint-disable comment and `const composeEnhancers = window.__REDUX_DEVTOOLS_EXTENSION_COMPOSE__ || compose`
    - Line 31: `const store = createStore(reducer, composeEnhancers(applyMiddleware(...middleware)))`

    Add this import (replace line 5):
    ```javascript
    import { configureStore } from '@reduxjs/toolkit'
    ```

    Add this store creation (replace lines 26-31):
    ```javascript
    const store = configureStore({ reducer })
    ```

    All other imports and code in App.jsx remain unchanged. The `reducer` default import from `'./reducers'` (line 14) is passed directly to `configureStore`. Per D-02, do NOT modify reducers/index.js or any reducer files.

    **Step 3 — Update vite.config.js manualChunks:**

    Change the `redux` chunk entry from:
    ```javascript
    redux: ['redux', 'react-redux', 'redux-thunk'],
    ```
    to:
    ```javascript
    redux: ['redux', 'react-redux', '@reduxjs/toolkit'],
    ```
    This prevents a stale chunk reference since `redux-thunk` is no longer directly imported (RTK bundles it internally).

    **Step 4 — Verify CLN-01 (per D-05):**
    Run: `grep -r "topic/items" planningpoker-web/src/`
    Expected: zero matches. The dead subscription does not exist. No code changes needed.

    **Step 5 — Verify UI-04 (per D-06):**
    Confirm `App.jsx` still contains the Suspense wrapper with CircularProgress fallback wrapping the Routes. This was already present before migration and should be unchanged.

    **Step 6 — Remove redux-thunk from package.json (per D-03 — removing unused direct dep):**
    ```bash
    cd planningpoker-web && npm uninstall redux-thunk
    ```
    RTK bundles thunk internally; the direct dependency is no longer imported by any source file after the App.jsx migration.
  </action>
  <verify>
    <automated>cd planningpoker-web && grep -c "configureStore" src/App.jsx && grep -c "createStore" src/App.jsx | grep "^0$" && grep -c "@reduxjs/toolkit" package.json && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep "configureStore" planningpoker-web/src/App.jsx` returns `import { configureStore } from '@reduxjs/toolkit'`
    - `grep "createStore" planningpoker-web/src/App.jsx` returns zero matches
    - `grep "applyMiddleware" planningpoker-web/src/App.jsx` returns zero matches
    - `grep "compose" planningpoker-web/src/App.jsx` returns zero matches (no `compose` or `composeEnhancers`)
    - `grep "redux-thunk" planningpoker-web/src/App.jsx` returns zero matches
    - `grep "REDUX_DEVTOOLS_EXTENSION" planningpoker-web/src/App.jsx` returns zero matches
    - `grep "@reduxjs/toolkit" planningpoker-web/package.json` returns a match
    - `grep "redux-thunk" planningpoker-web/package.json` returns zero matches in dependencies
    - `grep "@reduxjs/toolkit" planningpoker-web/vite.config.js` returns a match in manualChunks
    - `grep "redux-thunk" planningpoker-web/vite.config.js` returns zero matches
    - `grep "topic/items" planningpoker-web/src/` returns zero matches (CLN-01 verified)
    - `grep "Suspense" planningpoker-web/src/App.jsx` returns a match (UI-04 verified)
    - `cd planningpoker-web && npm run build` succeeds with exit code 0
  </acceptance_criteria>
  <done>
    - App.jsx uses configureStore from @reduxjs/toolkit (CLN-03)
    - All legacy createStore/applyMiddleware/compose/composeEnhancers/thunk imports removed
    - redux-thunk removed from package.json dependencies
    - vite.config.js manualChunks updated to reference @reduxjs/toolkit instead of redux-thunk
    - /topic/items subscription confirmed absent (CLN-01)
    - Suspense fallback confirmed present (UI-04)
    - Frontend builds successfully
  </done>
</task>

<task type="auto">
  <name>Task 2: Run all tests and verify no regressions</name>
  <files>planningpoker-web/src/App.jsx</files>
  <read_first>
    - planningpoker-web/src/App.jsx (verify final state after Task 1)
  </read_first>
  <action>
    **Step 1 — Run frontend lint:**
    ```bash
    cd planningpoker-web && npm run lint
    ```
    Verify no ESLint errors. If there are unused import warnings (e.g., leftover `thunk` import), fix them — but Task 1 should have already removed them.

    **Step 2 — Run frontend build (already done in Task 1 verify, but confirm):**
    ```bash
    cd planningpoker-web && npm run build
    ```

    **Step 3 — Run backend build and tests (to confirm nothing is broken end-to-end):**
    ```bash
    cd /Users/richard/Projects/planningpoker && ./gradlew planningpoker-web:jar
    ```
    Then in a separate invocation:
    ```bash
    ./gradlew planningpoker-api:build
    ```
    These must be separate Gradle invocations per CLAUDE.md instructions.

    **Step 4 — Confirm RTK serializable check middleware does not flag existing actions:**
    This can only be verified at runtime. Note in the summary that the dev server should be checked for console warnings about non-serializable values after migration. If warnings appear during E2E tests or manual testing, they indicate action payloads that need attention (but are not blocking).
  </action>
  <verify>
    <automated>cd /Users/richard/Projects/planningpoker/planningpoker-web && npm run lint && npm run build && cd /Users/richard/Projects/planningpoker && ./gradlew planningpoker-web:jar && ./gradlew planningpoker-api:build</automated>
  </verify>
  <acceptance_criteria>
    - `cd planningpoker-web && npm run lint` exits with code 0
    - `cd planningpoker-web && npm run build` exits with code 0
    - `./gradlew planningpoker-web:jar` exits with code 0
    - `./gradlew planningpoker-api:build` exits with code 0 (includes backend tests)
  </acceptance_criteria>
  <done>
    - Frontend linting passes with no errors
    - Frontend builds successfully
    - Backend builds and all backend tests pass
    - Full build chain (frontend build -> web JAR -> API build) completes without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This phase modifies build-time infrastructure (Redux store setup) only. No changes to input handling, API surface, authentication, or data flow.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | npm dependency (@reduxjs/toolkit) | accept | Official Redux team package with 30M+ weekly downloads; verified version from npm registry; lockfile pins exact version |
</threat_model>

<verification>
1. `grep -c "configureStore" planningpoker-web/src/App.jsx` returns 1+
2. `grep -c "createStore\|applyMiddleware\|compose" planningpoker-web/src/App.jsx` returns 0
3. `grep -r "topic/items" planningpoker-web/src/` returns 0 matches
4. `grep -c "Suspense" planningpoker-web/src/App.jsx` returns 1+
5. `cd planningpoker-web && npm run lint && npm run build` both exit 0
6. `./gradlew planningpoker-web:jar` then `./gradlew planningpoker-api:build` both exit 0
</verification>

<success_criteria>
- Redux store uses `configureStore` from `@reduxjs/toolkit` (CLN-03 complete)
- No `createStore`, `applyMiddleware`, `compose`, or `__REDUX_DEVTOOLS_EXTENSION_COMPOSE__` in App.jsx
- `redux-thunk` removed from direct dependencies
- `/topic/items` subscription confirmed absent from source (CLN-01 verified)
- Suspense fallback with CircularProgress confirmed present (UI-04 verified)
- All linting, frontend build, backend build, and backend tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-frontend-modernization/10-01-SUMMARY.md`
</output>
