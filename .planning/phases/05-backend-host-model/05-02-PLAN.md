---
phase: 05-backend-host-model
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - planningpoker-api/src/main/java/com/richashworth/planningpoker/model/SessionResponse.java
  - planningpoker-api/src/main/java/com/richashworth/planningpoker/controller/GameController.java
  - planningpoker-api/src/test/java/com/richashworth/planningpoker/controller/GameControllerTest.java
autonomous: true
requirements:
  - HOST-01
  - HOST-02
must_haves:
  truths:
    - "createSession response includes the host username"
    - "joinSession response includes the current host username"
    - "refresh triggers a users message that includes host info"
    - "After host leaves, the next API call reflects the new host"
  artifacts:
    - path: "planningpoker-api/src/main/java/com/richashworth/planningpoker/model/SessionResponse.java"
      provides: "SessionResponse record with host field"
      contains: "String host"
    - path: "planningpoker-api/src/main/java/com/richashworth/planningpoker/controller/GameController.java"
      provides: "Controllers that pass host to SessionResponse"
      contains: "getHost"
    - path: "planningpoker-api/src/test/java/com/richashworth/planningpoker/controller/GameControllerTest.java"
      provides: "Tests verifying host in responses"
      contains: "response.host()"
  key_links:
    - from: "GameController.createSession"
      to: "SessionManager.getHost"
      via: "fetches host after registration to include in response"
      pattern: "sessionManager\\.getHost"
    - from: "GameController.joinSession"
      to: "SessionManager.getHost"
      via: "fetches host to include in join response"
      pattern: "sessionManager\\.getHost"
---

<objective>
Expose host identity in API responses so clients know who the host is. Update SessionResponse to include a `host` field, and update createSession, joinSession endpoints to populate it.

Purpose: Clients need the host identity to render host indicators and controls (Phases 6-7). This plan wires the SessionManager host data (from Plan 01) into the REST API contract.
Output: Updated SessionResponse with host field, updated controllers and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-host-model/05-01-SUMMARY.md

@planningpoker-api/src/main/java/com/richashworth/planningpoker/model/SessionResponse.java
@planningpoker-api/src/main/java/com/richashworth/planningpoker/controller/GameController.java
@planningpoker-api/src/test/java/com/richashworth/planningpoker/controller/GameControllerTest.java
@planningpoker-api/src/test/java/com/richashworth/planningpoker/controller/AbstractControllerTest.java
@planningpoker-api/src/test/java/com/richashworth/planningpoker/common/PlanningPokerTestFixture.java

<interfaces>
<!-- From Plan 01 (SessionManager additions) -->
From SessionManager.java (added by Plan 01):
```java
public String getHost(String sessionId)    // returns host username or null
public void setHost(String sessionId, String userName)  // explicit host assignment
```

<!-- Current SessionResponse (will be modified) -->
From SessionResponse.java:
```java
public record SessionResponse(
        String sessionId,
        String schemeType,
        List<String> values,
        boolean includeUnsure,
        boolean includeCoffee
) {}
```

<!-- Current controller patterns -->
From GameController.java:
```java
// createSession returns: new SessionResponse(sessionId, schemeConfig.schemeType(), values, ...)
// joinSession returns: new SessionResponse(null, config.schemeType(), values, ...)
```

<!-- Test fixtures -->
From PlanningPokerTestFixture.java:
```java
public static final String SESSION_ID = "abc12345";
public static final String USER_NAME = "Rich";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add host field to SessionResponse and update API endpoints</name>
  <files>
    planningpoker-api/src/main/java/com/richashworth/planningpoker/model/SessionResponse.java,
    planningpoker-api/src/main/java/com/richashworth/planningpoker/controller/GameController.java,
    planningpoker-api/src/test/java/com/richashworth/planningpoker/controller/GameControllerTest.java
  </files>
  <read_first>
    planningpoker-api/src/main/java/com/richashworth/planningpoker/model/SessionResponse.java
    planningpoker-api/src/main/java/com/richashworth/planningpoker/controller/GameController.java
    planningpoker-api/src/test/java/com/richashworth/planningpoker/controller/GameControllerTest.java
    planningpoker-api/src/test/java/com/richashworth/planningpoker/controller/AbstractControllerTest.java
    planningpoker-api/src/test/java/com/richashworth/planningpoker/common/PlanningPokerTestFixture.java
  </read_first>
  <behavior>
    - Test: testCreateSession asserts response.host() equals USER_NAME ("Rich")
    - Test: testJoinSession asserts response.host() equals the host username (mock getHost to return "HostUser")
    - Test: testCreateSessionWithTshirtScheme asserts response.host() equals USER_NAME
    - Test: testLeaveSession verifies sessionManager.getHost is NOT called (leave returns void, host promotion is internal to SessionManager)
  </behavior>
  <action>
    1. Update SessionResponse record to add `host` as the FIRST field (before sessionId) for clean JSON output:
       ```java
       public record SessionResponse(
               String host,
               String sessionId,
               String schemeType,
               List<String> values,
               boolean includeUnsure,
               boolean includeCoffee
       ) {}
       ```

    2. Update GameController.createSession to include host in response:
       - After `messagingUtils.burstUsersMessages(sessionId)`, add: `String host = sessionManager.getHost(sessionId);`
       - Change the return to: `return new SessionResponse(host, sessionId, schemeConfig.schemeType(), values, schemeConfig.includeUnsure(), schemeConfig.includeCoffee());`

    3. Update GameController.joinSession to include host in response:
       - After `messagingUtils.burstUsersMessages(sessionId)`, add: `String host = sessionManager.getHost(sessionId);`
       - Change the return to: `return new SessionResponse(host, null, config.schemeType(), values, config.includeUnsure(), config.includeCoffee());`

    4. Update GameControllerTest:
       - In testCreateSession: Add `when(sessionManager.getHost(SESSION_ID)).thenReturn(USER_NAME);` and assert `assertEquals(USER_NAME, response.host());`
       - In testJoinSession: Add `when(sessionManager.getHost(SESSION_ID)).thenReturn("HostUser");` and assert `assertEquals("HostUser", response.host());`
       - In testCreateSessionWithTshirtScheme: Add `when(sessionManager.getHost(SESSION_ID)).thenReturn(USER_NAME);` and assert `assertEquals(USER_NAME, response.host());`
       - Existing testLeaveSession, testRefresh, testReset need no changes (they don't check SessionResponse).

    5. Verify all existing tests in GameControllerTest still pass. The mock for `sessionManager.getHost()` must be added to every test that calls createSession or joinSession on the controller, because Mockito strict stubs will fail if getHost is called but not stubbed.
  </action>
  <verify>
    <automated>cd /Users/richard/Projects/planningpoker && ./gradlew planningpoker-api:test -x :planningpoker-web:jar</automated>
  </verify>
  <acceptance_criteria>
    - SessionResponse.java contains `String host` as a record component
    - GameController.java createSession method contains `sessionManager.getHost(sessionId)`
    - GameController.java joinSession method contains `sessionManager.getHost(sessionId)`
    - GameControllerTest.java testCreateSession contains `response.host()`
    - GameControllerTest.java testJoinSession contains `response.host()`
    - All GameControllerTest tests pass (exit code 0)
    - All SessionManagerTest tests still pass (exit code 0)
  </acceptance_criteria>
  <done>
    - SessionResponse includes host field
    - createSession response contains the creator as host
    - joinSession response contains the current session host
    - All controller and service tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → REST API | Client receives host identity in SessionResponse; cannot set it directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | SessionResponse.host | accept | Host username is already visible in the users list broadcast over WebSocket. Including it in SessionResponse adds no new information exposure. |
| T-05-05 | Spoofing | host field in response | mitigate | Host field is read-only in the response (server sets it from SessionManager.getHost). No client-supplied host value is accepted by any endpoint. |
</threat_model>

<verification>
```bash
cd /Users/richard/Projects/planningpoker && ./gradlew planningpoker-api:test -x :planningpoker-web:jar
```
All controller + service tests pass. SessionResponse JSON output includes `host` field.
</verification>

<success_criteria>
- SessionResponse includes host field populated by server
- createSession returns creator as host
- joinSession returns current host for the session
- No client can set host through API (server-authoritative)
- All existing + new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-backend-host-model/05-02-SUMMARY.md`
</output>
